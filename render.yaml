services:
  - type: web
    name: meteor
    env: node
    envVars:
      - key: NODE_VERSION
        value: 18.9.0
    repo: 
    plan: free
    buildCommand: npm install
    startCommand: node .
    rootDir: ./

